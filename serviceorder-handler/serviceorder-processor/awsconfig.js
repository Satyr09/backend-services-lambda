const constants = require("../../broadcast-service/communication-channels/constants")

const config = {
    SERVICE_ORDER_TABLE_NAME: "ServiceOrder",
    REGION : "ap-south-1"
}

module.exports = config;